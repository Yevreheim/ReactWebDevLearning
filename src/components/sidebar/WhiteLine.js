//import 'whiteline.css';
import styled from '@emotion/styled'
let WhiteLine = styled.div
`
    width: ${props => props.width};
    height: ${props => props.height};
    backgroundColor: ${props => props.backgroundColor};
`
export default WhiteLine;

